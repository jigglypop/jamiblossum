# Astra/Sol 실행 하네스

실행 설정은 `.codex/config.toml`에 있다. 상세 운영 문서 `docs/harness.md`는 적용 문제나 비용 측정 때만 읽는다.

## 역할과 원칙

- 루트 `gpt-6-astra`는 목표와 제약 확정, 작업 분해, 중요한 판단, 충돌 조정, 핵심 검수만 맡는다.
- 실행은 기본적으로 `gpt-5.6-sol` 한 명이 탐색부터 구현과 검증까지 이어서 맡는다. 서로 독립적이고 병렬 이득이 분명한 작업만 Sol을 최대 두 명까지 사용한다.
- 하위 에이전트는 재위임하지 않는다. 같은 파일을 둘 이상에게 동시에 맡기지 않는다.
- 비용 절감 효과는 측정하기 전에는 확언하지 않는다.
- 새 탐색이나 구현이 없는 아주 짧은 응답은 위임 비용을 피하도록 루트가 직접 처리한다.
- 불필요한 문구는 추가하지 않는다

## 명반
- 명반의 정확도를 검증한다