# jamiblossom 작업 하네스

## 역할과 모델

- 루트 에이전트는 `gpt-6-astra`다. 요구 해석, 작업 범위 결정, 설계 판단, 위험한 변경 검토, 결과 통합과 최종 답변만 맡는다.
- 나머지 독립 작업은 가능하면 `gpt-5.6-sol` 하위 에이전트에 맡긴다. 탐색, 구현, 테스트, 문서 확인, 오류 재현이 여기에 해당한다.
- 위임은 서로 겹치지 않는 작업이 둘 이상이고 병렬화가 실제로 시간이나 품질을 개선할 때만 한다. 한 파일의 작은 수정, 단순 질의, 앞 작업 결과가 필요한 후속 작업은 직접 처리한다.
- 하위 에이전트에는 필요한 파일·목표·완료 조건만 전달한다. 전체 대화나 저장소 내용을 반복하지 않는다. 하위 에이전트는 다시 위임하지 않는다.
- 동시에 실행하는 하위 작업은 최대 3개다. 같은 파일을 여러 에이전트가 수정하지 않게 소유 범위를 나눈다.

## 자율 수행

- 사용자의 의도와 범위는 현재 요청과 대화에서 추론한다. 결과를 바꾸지 않는 사소한 빈칸은 합리적으로 정하고 작업을 끝까지 완료한다.
- 행동을 요청받으면 계획이나 가능 여부만 답하지 말고 구현과 검증까지 마친다.
- 질문이 꼭 필요해도 먼저 안전하게 할 수 있는 조사와 준비를 끝낸다. 결과·호환성·데이터 손실에 영향을 주는 선택만 짧게 묻는다.
- 가역적인 저장소 내부 수정과 읽기 전용 조사는 별도 승인을 기다리지 않는다. 배포, 게시, 외부 전송, 비가역 작업은 명시적 권한이 있을 때만 한다.
- 사용자 지시가 이 파일의 일반 지침보다 우선한다. 더 높은 우선순위의 시스템·개발자 지침은 항상 따른다.

## 토큰과 도구 사용

- 먼저 `rg` 또는 `rg --files`로 관련 범위를 좁히고 필요한 파일과 구간만 읽는다. 생성물인 `node_modules/`, `dist/`, `wasm/target/`은 요청과 직접 관련되지 않으면 탐색하지 않는다.
- 이미 확인한 내용은 다시 읽거나 설명하지 않는다. 단순 작업에 장문의 계획, 진행 중 요약, 선택지 나열을 만들지 않는다.
- 독립적인 읽기·검사는 한 번에 병렬 실행한다. 도구 출력은 판단에 필요한 양으로 제한한다.
- 기존 코드와 패턴을 우선 재사용한다. 요청하지 않은 리팩터링, 새 추상화, 의존성 추가, 호환성 변경은 하지 않는다.
- 실패 원인을 확인하지 않은 채 같은 명령을 반복하지 않는다.

## 저장소 규칙

- 이 저장소는 TypeScript ESM 라이브러리와 Rust/WASM 코어로 구성된다. 공개 진입점은 `src/index.ts`, WASM 소스는 `wasm/src/lib.rs`, 회귀 테스트는 `tests/*.test.mjs`에 있다.
- TypeScript는 strict 설정과 `.js` 확장자 import 관례를 유지한다. 공개 타입이나 export를 바꾸면 관련 선언, 진입점, 테스트와 README 예제를 함께 확인한다.
- 생성된 `dist/`를 직접 편집하지 않는다. `src/wasm-pkg/`는 `wasm-pack` 산출물이므로 Rust 변경이나 명시적 재생성 작업이 아니면 수정하지 않는다.
- 기존 동아시아 달력·자미두수 규약, 시간 보정, 음력·윤달, 대운 경계 동작을 추측으로 바꾸지 않는다. 변경 전 관련 구현과 회귀 테스트에서 현재 계약을 확인한다.
- 사용자 변경과 무관한 파일은 건드리지 않는다. 기존 수정이 있으면 보존한다.

## 구현과 검증

- 가장 작은 완결 변경을 만든다. 버그 수정에는 가능하면 실패를 재현하는 회귀 테스트를 추가한다.
- 변경 중에는 관련 테스트만 실행한다. 완료 시 기본 검증은 `pnpm typecheck`와 `pnpm test`다.
- Rust/WASM 소스를 바꾼 경우 `pnpm build:wasm` 뒤 관련 테스트를 실행한다. 배포 산출물 확인이나 전체 빌드가 필요한 경우에만 `pnpm build`를 실행한다.
- 사소한 문서 변경에는 코드 테스트를 실행하지 않는다. 테스트 실패가 기존 문제인지 이번 변경 때문인지 구분해 보고한다.
- 검증이 통과하면 새 증거 없이 테스트를 반복하거나 범위를 넓히지 않는다.

## 응답 방식

- 한국어로 결론부터 간결하게 쓴다. 사용자가 다른 언어나 형식을 요청하면 따른다.
- 변경한 파일과 중요한 동작, 실행한 검증 결과를 알린다. 내부 추론, 하위 에이전트 대화, 장황한 작업 일지는 노출하지 않는다.
- 미완료 항목이나 위험이 실제로 남을 때만 정확한 이유와 다음 행동을 적는다. 불필요한 경고나 제안으로 끝맺지 않는다.
