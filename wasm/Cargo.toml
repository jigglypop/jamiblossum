[package]
name = "jamiblossom-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "WASM core for jamiblossom Zi Wei and calendar calculations."

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wasm-bindgen = "0.2"
